require "math"

describe('Testing greet()',function ( ... )
	
-- 	2.
	it('Trying to produce dissimilar outputs',function ( ... )
		local expected = "Das Boot"
		local got = correctAnswer

		assert.are.same(expected, got)
	end)

end)
